# UI Placeholder

This folder will hold the web-native dashboard.

Initial target:

- local SPA
- talks to the Rust server over HTTP
- supports dataset import, preflight review, bucket-plan preview, and result inspection
